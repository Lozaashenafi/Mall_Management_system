<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Utility Invoice <%= invoice.id %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        padding: 20px;
        color: #333;
      }
      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }
      .logo {
        width: 80px;
        height: 80px;
        background: #ccc;
        border-radius: 50%;
        text-align: center;
        line-height: 80px;
        font-weight: bold;
      }
      .section {
        margin-bottom: 20px;
      }
      .section h3 {
        margin-bottom: 5px;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 10px;
      }
      table th,
      table td {
        border: 1px solid #ccc;
        padding: 8px;
        text-align: left;
      }
      table th {
        background-color: #f2f2f2;
      }
      .text-right {
        text-align: right;
      }
      .balance-due {
        margin-top: 20px;
        padding: 10px;
        background-color: #333;
        color: white;
        width: fit-content;
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <div>
        <h2>Your Company Name</h2>
        <p>Your Address</p>
        <p>Contact Details</p>
      </div>
      <div class="logo">LOGO</div>
    </div>

    <div class="section">
      <h3>Utility Invoice</h3>
      <p><strong>Invoice No:</strong> <%= invoice.id %></p>
      <p><strong>Utility Type:</strong> <%= invoice.utilityCharge.type %></p>
      <p><strong>Billing Month:</strong> <%= invoice.utilityCharge.month %></p>
      <p>
        <strong>Date:</strong>
        <%= new Date(invoice.createdAt).toLocaleDateString() %>
      </p>
    </div>

    <div class="section" style="display: flex; justify-content: space-between">
      <div>
        <h3>Bill To</h3>
        <p><%= invoice.rental?.tenant?.contactPerson || "N/A" %></p>
        <p><%= invoice.rental?.tenant?.companyName || "N/A" %></p>
        <p><%= invoice.rental?.tenant?.address || "N/A" %></p>
        <p><%= invoice.rental?.tenant?.phone || "N/A" %></p>
        <p><%= invoice.rental?.tenant?.email || "N/A" %></p>
      </div>
      <div>
        <h3>Room Info</h3>
        <p>Room: <%= invoice.rental?.room?.unitNumber || "N/A" %></p>
        <p>Floor: <%= invoice.rental?.room?.floor || "N/A" %></p>
      </div>
    </div>

    <table>
      <tr>
        <th>Description</th>
        <th>Amount (ETB)</th>
      </tr>
      <tr>
        <td>
          <%= invoice.utilityCharge.description || invoice.utilityCharge.type %>
        </td>
        <td><%= invoice.amount.toFixed(2) %></td>
      </tr>
    </table>

    <div class="section text-right">
      <p><strong>Total:</strong> <%= invoice.amount.toFixed(2) %> ETB</p>
    </div>

    <div class="balance-due">
      Balance Due: <%= invoice.amount.toFixed(2) %> ETB
    </div>

    <div class="section">
      <p><strong>Status:</strong> <%= invoice.status %></p>
      <p>
        <strong>Remarks:</strong> This utility invoice was automatically
        generated for your <%= invoice.utilityCharge.type %> usage for <%=
        invoice.utilityCharge.month %>.
      </p>
    </div>
  </body>
</html>
